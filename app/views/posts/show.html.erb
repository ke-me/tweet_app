
<p><%= safe_join(@post.content.split("\n"),tag(:br)) %></p>
<% if current_user && @post.user_id == current_user.id %>
<div><%= link_to "削除", post_path(@post), data: { turbo_method: :delete } %></div>
<% end %>