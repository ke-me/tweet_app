<div class="post-list">
    <div class="post-item">
        <% @posts.each do |post| %>
            <%= link_to post do %>
                <p><%= safe_join(post.content.split("\n"),tag(:br)) %>（<%= post.user.name %>）</p>
            <% end %>
        <% end %>
    </div>
</div>